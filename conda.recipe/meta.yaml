package:
  name: nbsetuptools
  version: '0.1.0'

requirements:
  build:
    - python

  run:
    - funcsigs # [py2]
    - notebook
    - python

source:
  path: ../

about:
  home: https://github.com/anaconda-server/anaconda-notebook
  license: TBD
